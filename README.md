## Webdriver automation of local browser

This is a showcase project that uses Selenium webdriver to drive browser on the local machine.
It features:
* Page Object Model design pattern
* Keywords to represent browser actions
* data driven tests
